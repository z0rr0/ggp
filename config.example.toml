[base]
timezone = "UTC"
admins = []
debug = false

[database]
path = "ggp.sqlite"
query_timeout = 5  # in seconds

[fetcher]
active = true
period = 300  # in seconds
token = "auth_token"
url = ""  # JSON http(s) url to data source

[holidayer]
active = true
period = 86400  # in seconds, 1 day
url = ""  # XML http(s) url to data source, year is <YEAR> string

[predictor]
active = true
hours = 4

[telegram]
active = true
token = "bot_token"
